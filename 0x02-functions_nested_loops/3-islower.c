#include "main.h"

/**
 * _islower -> checks if the character is lowercase
 * int c: character defining
 * Return: returns 1 and 0 depending on conditions
 */

int _islower(int c)

{
	return (c >= 'a' && c <= 'z');
}
