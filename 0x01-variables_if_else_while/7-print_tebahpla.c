#include <stdio.h>
/**
 * main - function parameter control
 * Return: always 0
 */

int main(void)
{
		int chr;

		for (chr = 122; chr >= 97; chr--)
		{
			putchar(chr);
		}
			putchar('\n');

			return (0);
}
